define([
    'underscore',
    'backbone'
    ], function(_, Backbone) {
        var storyModel = Backbone.Model.extend({
            // TODO: Add definitions here
        });
        return new storyModel();
    }
);
